import React, { memo } from 'react';
import Header from '../components/organism/Header';

const Home = () => {
    return (
        <>
            <Header />
        </>
    );
}:

export default memo(Home);